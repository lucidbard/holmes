declare var VTTCue: any;
declare interface Math {
        trunc(num?: number):number;
}
declare var Clipboard: any;
declare function parseFloat(str: String, radix: number);

declare interface Function {
done: boolean;
}